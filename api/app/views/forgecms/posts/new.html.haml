=form_for @post, :builder => Forgecms::FormBuilder do |form|
  .post-content
    = form.text_field :title, { class: "title", placeholder: "New Post", autocomplete: "off" }
    = form.text_area :body, { class: "body" }
  =render 'post_sidebar', :form => form